# LeadFlow Deep Audit — 2026-05-28

**Pinned commit:** `e5dfbc2` (main)
**Pinned migration (local newest):** `20260527090000_sourcing_cron_dispatcher.sql` (56 files)
**Pinned migration (remote newest):** `20260527063549_vcglr_licences` (56 rows — name drift, see P1-DB-01)
**Project ref:** `bsevgxhnxlkzkcalevbb` (Sydney `ap-southeast-2`)
**Audit start → close:** 09:08 → 09:24 UTC (~17 min, 4 parallel investigation agents)
**Sentry status:** runtime SDK wired (PR #91), source-map upload **not wired** (see P1-ENV-01)
**Codex availability:** quota-out today on `gpt-5.5` and `gpt-5-codex` — PR #90 (VCGLR) shipped without Pattern B review (P1-OPS-01)

---

## TL;DR — cold-send readiness verdict

**CONDITIONAL — leaning NO-GO until 10/06/2026.** Plumbing is solid: org consolidation applied (1 org confirmed), 16 active crons with 0 failures in 24h, warmup worker live and ticking, RLS multi-tenancy intact on every public table, no plaintext keys in repo, suppression + cold-outreach guard + RFC 8058 unsubscribe headers all firing. **But three hard blockers stand between today and a real cold-send:** (1) the Spam Act sender block on `users` is almost certainly still the seed-migration placeholder `ABN 12 345 678 901, PO Box 123, Melbourne VIC 3000` — sending with that footer breaches Spam Act 2003 s.17; (2) warmup network is on Day 1 of a mandatory 14-day ramp per PR #88 ship summary — **earliest safe cold-send is 10/06/2026**; (3) the ICP brief specified "7-council allowlist + employee-band gating" but neither exists in code — sourcing returns whatever Outscraper/Google Places hands back inside the suburb whitelist. Add 4 missing Edge Function env vars (`PIXEL_BASE_URL`, `PUBLIC_APP_URL`, `EMAIL_VERIFICATION_API_KEY`, `_PROVIDER`) and wire `@sentry/vite-plugin` so production stack traces are readable, and the picture flips from NO-GO to GO on the warmup-floor date.

---

## P0 — ship-blockers before first cold-send

| # | Finding | Surface | Evidence | Proposed fix | Effort |
|---|---|---|---|---|---|
| P0-01 | **Placeholder ABN + PO Box in Spam-Act sender block likely still live.** Seed migration writes literal `Jordan Marziale - Premium Water AU - ABN 12 345 678 901 - PO Box 123, Melbourne VIC 3000` into `users.spam_act_sender_block`; cold sends will go out with a fictional ABN + fake address. Spam Act 2003 s.17 violation. | DB → SMTP footer | `supabase/migrations/20260519000003_warmup_and_spam_act.sql:213-223`; `supabase/functions/send-via-smtp/index.ts:175-183`, `:236-239` | `UPDATE public.users SET spam_act_sender_block = '<real Jordan ABN + real address>' WHERE email = 'demo@jordan-sales-agent.test'` via Settings UI or SQL. Verify via `SELECT spam_act_sender_block FROM users;` | 10 min |
| P0-02 | **Warmup is on Day 1 of mandatory 14-day ramp.** Per PR #88 ship summary, first warmup send fired 27/05 09:00 AEST. Earliest safe cold-send = 10/06/2026. Cold-sending before that risks all 4 inboxes hitting Google Postmaster "Bad" reputation grade. | Operational timing | `clients/jordan/plans/warmup-network-fix-ship-summary.md:9,84`; live `email_accounts.warmup_day=2` confirmed via DB probe; warmup cron `*/30` runs in 24h | Hold first cold-send until 10/06/2026. Monitor `email_accounts.warmup_day` daily — must reach ≥10 before opening cold-send tap. | wall-clock only |
| P0-03 | **ICP filter has no LGA allowlist and no employee-band gating.** Brief specified 7 inner-Melbourne councils + employee bands; grep returns zero LGA/council/employee_count references in `src/` or `supabase/functions/`. Category whitelist is enforced **only** in the saved-search creation UI; `discover-leads` accepts `categories` from DB row verbatim and a `category=NULL` Google Places result will pass straight through to a draftable contact. | Sourcing → ICP | `src/lib/constants/hospitality-categories.ts:8-21`; `supabase/functions/discover-leads/index.ts:262,407-413,420-428` | Two options: (a) confirm with Jordan that LGA/employee gating is out of scope for v1; or (b) build it — add `lead_searches.lga_allowlist text[]` + employee enrichment via Apollo/Proxycurl + filter at draft-generate time. Add CHECK constraint forcing `categories` to be a subset of the hospitality whitelist. | (a) 5 min; (b) 1–2 days |

---

## P1 — fix this week (before scale beyond first 50 sends)

### Secrets / env

| # | Finding | Surface | Evidence | Proposed fix | Effort |
|---|---|---|---|---|---|
| P1-ENV-01 | **Sentry source-map upload not configured.** PR #91 wired `@sentry/react` runtime, but `package.json` has no `@sentry/vite-plugin`, `vite.config.ts` is bare, and Vercel prod lacks `SENTRY_AUTH_TOKEN`/`SENTRY_ORG`/`SENTRY_PROJECT`. First real-prospect-side error will surface as minified `aB.xY:1234` garbage. | Vercel + Vite | `package.json` (only `@sentry/react ^10.54.0`); `vite.config.ts` (no Sentry plugin); `vercel env ls production` shows 14 vars, none Sentry-build-related | `npm i -D @sentry/vite-plugin`; add `sentryVitePlugin({ org, project, authToken })` to `vite.config.ts`; set 3 Vercel build-env vars. | 30 min |
| P1-ENV-02 | **`EMAIL_VERIFICATION_API_KEY` + `EMAIL_VERIFICATION_PROVIDER` missing from Edge Function secrets.** `enqueue-sends` reads these to call ZeroBounce pre-flight; without them it falls back to no pre-flight verification (bounce-only). `ZEROBOUNCE_API_KEY` is stored in Vercel prod (wrong place — not read there). | Supabase Edge secrets | `supabase/functions/enqueue-sends/index.ts:45,47`; `supabase secrets list` | `supabase secrets set EMAIL_VERIFICATION_API_KEY=<zerobounce-key> EMAIL_VERIFICATION_PROVIDER=zerobounce`. Optionally remove the unused `ZEROBOUNCE_API_KEY` from Vercel. | 5 min |
| P1-ENV-03 | **`PIXEL_BASE_URL` missing from Edge secrets.** Open-tracking pixel falls back to default host; cold-send prospects receive emails pointing at the wrong tracking URL. | Edge | `supabase/functions/drain-send-queue/index.ts:147`, `send-via-smtp/index.ts:45` | `supabase secrets set PIXEL_BASE_URL=https://premiumwaterau.com/_t/` (or chosen pixel host) | 5 min |
| P1-ENV-04 | **`PUBLIC_APP_URL` missing from Edge secrets.** Four functions (`click-redirect`, `generate-draft`, `notify-warm-reply`, `sequence-tick`) read it for unsubscribe / click-redirect / WhatsApp deep-links. Falls back to `https://jordan-sales-agent.vercel.app` — non-brand URL visible to prospects in the unsub link. | Edge | `supabase/functions/generate-draft/index.ts:13` | `supabase secrets set PUBLIC_APP_URL=https://premiumwaterau.com.au` (or whichever brand domain is canonical for Jordan) | 5 min |

### Database / data integrity

| # | Finding | Surface | Evidence | Proposed fix | Effort |
|---|---|---|---|---|---|
| P1-DB-01 | **Remote-only migration `20260527063549_vcglr_licences` has no local file.** 23k+ row table exists remotely but a fresh clone replay won't recreate it. Two other migrations show local-vs-remote version-stamp mismatches (apply order via MCP rather than file). | Schema drift | `mcp__supabase__list_migrations` vs `supabase/migrations/` | `supabase db pull --linked` to capture the missing migration as a squash file. Commit. Do NOT run `supabase migration repair`. | 20 min |
| P1-DB-02 | **2 SECURITY DEFINER views flagged ERROR by Supabase advisor:** `public.cron_job_run_status` and `public.public_user_profiles`. | Security | `mcp__supabase__get_advisors type=security` | Drop SECURITY DEFINER from both views OR replace with restricted SECURITY INVOKER versions. | 30 min |
| P1-DB-03 | **Suppression `source='unsubscribe'` may violate CHECK constraint.** `classify-reply-intent` inserts auto-unsubscribe rows with `source='unsubscribe'`, but `suppression_list_source_check` (migration `20260519000003_warmup_and_spam_act.sql:60-72`) may not list that value — could silently fail INSERT. | Edge → DB | `supabase/functions/classify-reply-intent/index.ts:215-237`; constraint at L60-72 of warmup migration | Verify the CHECK constraint includes `'unsubscribe'`; if not, ALTER the constraint or change the insert to `'manual'`/`'reply'`. Add a probe to `scripts/smoke-classify-reply-suppression.sh` that asserts insert succeeds. | 20 min |
| P1-DB-04 | **Sender count expectation mismatch: code has 4 mailboxes, plan referenced 12.** Daily cap budget at 4×50 = 200/day; if the cold-send plan assumes 12×50 = 600/day, the cap throttles silently at the 200 mark with skipped drafts staying 'approved' on `enqueue-sends`. | Operational | DB: 4 active `email_accounts` rows; `clients/jordan/plans/warmup-network-fix-ship-summary.md:24-27` | Confirm 4-mailbox model with Jordan. If 12 is target, scope a Week-2 build to add 8 more domains/mailboxes (with 14-day warmup re-clock). | 5 min decision / 1–2 weeks if scaling |

### Workers + observability

| # | Finding | Surface | Evidence | Proposed fix | Effort |
|---|---|---|---|---|---|
| P1-OBS-01 | **`/admin/workers` missing 5 cold-send workers.** `workersConfig.ts` only knows 4 workers (`morning_briefing`, `reopening_radar_poll`, `learning_digest`, `sequence_tick`). New workers (`send-warmup-tick`, `poll-replies`, `enqueue-sends`, `drain-send-queue`, `process-bounces`) are invisible to the operator dashboard. | Frontend | `src/lib/workersConfig.ts:12-17` | Add 5 entries to `WORKER_EXPECTED_INTERVALS`. For `send-warmup-tick`, additionally add a widget that reads `email_send_events WHERE metadata->>'kind'='warmup'` (warmup never writes `worker_runs`). | 45 min |
| P1-OBS-02 | **Edge Functions have zero alerting layer.** No `@sentry/deno` in `send-via-smtp`, `drain-send-queue`, `send-warmup-tick`, `poll-replies`. Errors go to `console.error` → Supabase function logs only; operator must actively pull logs to notice failure. | Edge | grep `@sentry` in `supabase/functions/` returns 0 | Add `@sentry/deno` init to the 4 critical send/warmup functions, wire same DSN as frontend. | 1 hour |

### Critical-path correctness

| # | Finding | Surface | Evidence | Proposed fix | Effort |
|---|---|---|---|---|---|
| P1-CP-01 | **Daily-cap is a 24h sliding window, not per-tz calendar day.** At Melbourne midnight `enqueue-sends` counts UTC-midnight-to-now; could spike sends just after Melbourne midnight if prior evening burned cap. | Edge | `supabase/functions/enqueue-sends/index.ts:235-237` ("per-user-tz day boundary is a Week 3 followup") | Replace the 24h-sliding count with a `WHERE event_at >= date_trunc('day', now() AT TIME ZONE users.send_timezone)` query. | 1 hour |
| P1-CP-02 | **`discover-leads` is `verify_jwt=false` + hand-rolled service-role check.** If `SUPABASE_SERVICE_ROLE_KEY` ever leaks to a Vite env (history says these things happen), the IDOR check at L364 is bypassable. | Edge | `scripts/smoke-manifest.yaml:63`; `discover-leads/index.ts:69-84,364-370` | Flip `verify_jwt=true` and use `requireServiceRoleAuth` (the pattern already used in `sourcing-cron-tick`). | 30 min |
| P1-CP-03 | **No automated SPF/DKIM/DMARC check.** All 4 sender domains' DNS state is operator-attested only. `scripts/check-dns.sh` does not exist. | Ops | grep `scripts/check-dns*` returns nothing; `clients/jordan/RUNBOOK-leadflow-sender-cutover.md:21-22` tells operator to `dig` manually | Add `scripts/check-dns.sh` that asserts `v=spf1` token, `p=` policy on `_dmarc.<domain>`, `default._domainkey.<domain>` for all 4 sender domains; exit non-zero on missing. Wire into the smoke run. | 1 hour |
| P1-CP-04 | **VCGLR sync ships with no failure visibility on parser drift.** Worker scrapes an HTML table by regex; if VCGLR changes layout, parser returns `[]` and `worker_runs` records `success_empty`, not `failed`. Looks healthy in dashboards while delivering zero leads. | Edge | `supabase/functions/vcglr-poll/index.ts:76-129` | Add a minimum-rows assertion (e.g. weekly run must return ≥10 rows) — record `worker_runs.status='failed'` with `error_text='parser_empty_drift_likely'` when below threshold. | 30 min |
| P1-CP-05 | **`crawl-venue-contacts` has no rate-limit / retry-on-429 / cost ceiling.** Hits arbitrary venue websites with Chrome UA; three fail modes silently swallow errors. No Sentry capture, no exponential backoff. | Edge | `supabase/functions/crawl-venue-contacts/index.ts:335-345,347-357,194-208` | Add per-domain rate limit (1 req/sec same host), 429 backoff, capture failures via Sentry. | 1.5 hours |
| P1-CP-06 | **Google Places quota has no cost ceiling.** `discover-leads` can page `next_page_token` up to `limit_per_run=1000` per category × 12 categories. At ~$17/1K Place Details requests, a single misconfigured saved-search burns >$200. | Edge | `supabase/functions/discover-leads/index.ts:211-279` | Add `lead_searches.daily_budget_usd` column; reject runs that would exceed it. Track cost in `lead_search_runs.cost_usd`. | 2 hours |
| P1-CP-07 | **`send-via-smtp` send logic is duplicated inside `drain-send-queue`.** Two send paths now exist with diverging behaviour (unsub footer building is similar but not byte-identical). When you change one, the other drifts. | Edge | `supabase/functions/drain-send-queue/index.ts:173-343` vs `send-via-smtp/index.ts:82-end` | Refactor `drain-send-queue` to call `send-via-smtp` as a subroutine, or extract shared `_shared/send-message.ts`. | 2 hours |
| P1-CP-08 | **Morning-briefing cron source migration still uses broken `current_setting('app.settings.service_role_key', true)`.** Production was patched in place via MCP (Vault pattern) but the source file is still broken — a fresh clone replay re-introduces the bug (silent 401 on every briefing). | Migration | `supabase/migrations/20260426000009_morning_briefing_cron.sql:39` (and adjacent `20260427000005_sequence_tick_cron.sql`) | Rewrite both source migrations to read from `vault.decrypted_secrets WHERE name='service_role_key'` directly. PR is open per `~/.claude/rules/dev/supabase-migrations.md`. | 30 min |
| P1-CP-09 | **`enqueue-sends` in-memory suppression `Set` doesn't strip `+alias`.** The DB trigger `email_drafts_suppression_guard` does (via `is_suppressed()` lowering + strip), so this is belt-and-braces — but pre-skip vs post-suppress inconsistency makes draft status flow confusing. | Edge | `supabase/functions/enqueue-sends/index.ts:264-310,299,303` | Normalise `recipient` with `lower(split_part(email,'+',1)||'@'||split_part(email,'@',2))` before the `Set.has` lookup. | 20 min |

### Ops

| # | Finding | Surface | Evidence | Proposed fix | Effort |
|---|---|---|---|---|---|
| P1-OPS-01 | **PR #90 (VCGLR sync) merged to main without Codex Pattern B review.** Both `gpt-5.5` and `gpt-5-codex` returned `ERROR: Quota exceeded` today. Per `codex-review.md` skipping requires Morty's explicit ship-anyway call; the PR body lists this as an outstanding follow-up, not a ship-anyway. | Process | PR #90 (`601f233`); follow-ups not in `clients/jordan/followups.md` | Refresh OpenAI quota; run `codex review --base main --head 601f233` retroactively. Address findings or accept-with-Morty-signoff. | 30 min after quota |
| P1-OPS-02 | **Draft-card suppression status visibility unverified before approve keystroke.** `useApproveDraft` re-reads `email_drafts` AFTER `'approved'` update to see if the trigger flipped it to `'suppressed'` — but if the **rendered** draft card doesn't show suppression status, Jordan could press A on a draft he'd never have approved if he saw the flag. Needs eyeball check. | Frontend | `src/pages/DraftsPage.tsx:1-432`; `src/lib/queries/drafts.ts:252-259` | Manual UI review — open `/drafts`, confirm the row card visibly shows suppression_reason if present. If missing, add a red badge. | 10 min check |

---

## P2 — hardening, fix in fortnight

| # | Finding | Surface | Proposed fix |
|---|---|---|---|
| P2-01 | **Anon role can EXECUTE 13 SECURITY DEFINER functions** including cold-send-critical `claim_send_queue_batch`, `select_next_sender`, `is_suppressed`. | DB | `REVOKE EXECUTE ON FUNCTION ... FROM anon, authenticated` on each. |
| P2-02 | **8 functions have `function_search_path_mutable`** (`haversine_km`, `set_updated_at`, `compute_deal_financials`, `sync_close_won_at`, `recompute_monthly_gate`, `trg_deals_recompute_gate_fn`, `run_monthly_gate_forfeits`, `touch_claude_conversation_updated_at`). | DB | Add `SET search_path = ''` to each. |
| P2-03 | **Unindexed FKs on cold-send-adjacent tables:** `email_send_queue_org_id_fkey`, `email_send_queue_user_id_fkey`, `email_drafts_contact_id/deal_id/created_by_fkey`, `email_send_events_draft_id_fkey`. | DB | Add covering indexes before crossing ~10k queue rows. |
| P2-04 | **`pg_net` extension installed in `public` schema.** | DB | Move to `extensions` schema. |
| P2-05 | **`auth_leaked_password_protection` disabled.** | Auth | Enable HaveIBeenPwned check. |
| P2-06 | **`claim_send_queue_batch` uses claim-via-status-field**, not `(claimed_at, claim_token)` columns the audit prompt assumed. Acceptable for `*/2 min` single-instance cron but the function's locking pattern should be re-verified for concurrent drain workers. | DB | Re-read function body, document concurrency model. |
| P2-07 | **`activities` index `(org_id, contact_id, created_at)` is partial.** Has `(contact_id, occurred_at DESC)` and `(org_id, activity_type, occurred_at DESC)` separately; cold-outreach guard uses `contact_id` lookup — verify query plan. | DB | `EXPLAIN ANALYZE` the cold-outreach guard SQL; add composite if plan is suboptimal. |
| P2-08 | **Local function dir `vcglr-poll/` vs deployed slug `vcglr-sync`.** Same feature renamed mid-stream; weekly cron hits deployed slug, repo has stale name. | Edge | `git mv supabase/functions/vcglr-poll supabase/functions/vcglr-sync`. |
| P2-09 | **Voice rules live on `users.voice_rules` free-text column.** No length cap, no schema; could prompt-inject Claude or burn tokens. | Edge | Add CHECK constraint `length(voice_rules) < 8000`. |
| P2-10 | **Cold-outreach guard counts ANY activity type** including `voice_note`, signal, reopening-radar promotion. Could be intended (any human touch ≠ cold) but worth confirming with Jordan. | Edge | Add `AND activity_type IN ('email_sent', 'reply_received', 'call', 'meeting')` filter, or document the broad-scope as intentional. |
| P2-11 | **Warmup write-path doesn't write `worker_runs`.** Events go to `email_send_events` per send — easy to miss for a worker-health dashboard. | Edge | Add a heartbeat `worker_runs` row per tick (success/empty/failed). |
| P2-12 | **Dead Gmail Pub/Sub code path still in repo** (`api/webhooks/gmail.ts`, `supabase/functions/gmail-inbound/`, `GMAIL_PUBSUB_TOKEN` references). | Repo hygiene | Delete dead code. Env vars correctly absent. |
| P2-13 | **`OAUTH_STATE_SECRET` + `GOOGLE_OAUTH_CLIENT_ID/SECRET` set in Vercel but only used by dormant Gmail-OAuth path** (replaced by IMAP). | Vercel env | Either delete from Vercel, or document why kept. |
| P2-14 | **Default `daily_send_cap = 50` is uniform across active and warming accounts** — warmup_day=1 mailbox would attempt 50 cold + 2 warmup. | DB | Make the cap a function of `warmup_day` (e.g. `least(50, warmup_day * 5)`). |
| P2-15 | **Working-hours mismatch: cold sends 08-18 default, warmup hard-capped 09-17.** If Jordan widens to 09-20, cold sends fire 17-20 while warmup is quiet — Gmail sees outbound spike without inter-inbox conversation. | Edge | Align constants or document. |
| P2-16 | **`send-via-smtp` httpsUrl for List-Unsubscribe-Post points at `unsubscribe-post` Edge Function** — verify handler exists and accepts the signed token. | Edge | Verify `unsubscribe-post` exists (it does — `supabase/functions/unsubscribe-post/`), assert token-acceptance smoke. |
| P2-17 | **Frontend Sentry replay disabled.** OK for v1; document if intended. | Frontend | `Sentry.replayIntegration()` add when ready. |
| P2-18 | **`VITE_APP_ENV` set in Vercel but no `src/` reference grepped.** Either unused or indirect-access — confirm. | Vercel env | Audit references or remove. |
| P2-19 | **`USERS.voice_rules` content filter** — no jailbreak scrub before injection into Claude system prompt. | Edge | Strip leading/trailing instructions; sandbox tokens. |
| P2-20 | **`200 multiple_permissive_policies` advisor finding.** RLS planner penalty at scale; harmless at current row counts. | DB | Consolidate policies (`USING` + `WITH CHECK` combined). |
| P2-21 | **`46 auth_rls_initplan` advisor findings.** Wrap `auth.uid()` in `(SELECT auth.uid())` to cache. | DB | Mechanical refactor. |
| P2-22 | **Synthetic-bounce backlog on `jordan@premiumwaterau.com.au`** (84 in 14d / 27 in 24h at audit time) — `detectInboxesAtRisk` will flag this inbox until cleared. | DB | `DELETE FROM email_send_events WHERE event_type='bounced' AND metadata->>'synthetic'='true'` (verify exact column). |

---

## P3 — nice-to-have / polish

- VCGLR weekly cron `leadflow-vcglr-sync-weekly` `last_run_at=null` — first run scheduled Mon 02/06 14:00 UTC, not a failure.
- `test_probe` vault entry from 24/04 — housekeeping, delete.
- `ZEROBOUNCE_API_KEY` in Vercel env but never read in `api/` — likely intended for Edge Function but stored in wrong place (closed by P1-ENV-02 fix).
- `vcglr_poll` + `vcglr_sync` single-event worker_runs failures in last 7d — not recurring.
- 30 unused indexes per advisor — drop after a month of real traffic.
- `pixel_track` doesn't redact IP — privacy enhancement.

---

## Reconciliation with prior audits

**Substitution note:** the audit prompt named `clients/jordan/plans/full-workflow-audit-2026-05-11.md` — **does not exist on disk** (confirmed via `find clients -name '*audit*'`). Substituted with three actual recent audit docs.

### Warmup network audit (26/05/2026) — verdict was FAIL

| Finding | Status | Evidence |
|---|---|---|
| Warmup Edge Function missing | ✅ CLOSED | `supabase/functions/send-warmup-tick/index.ts` exists (PR #88 `b8abb7c`) |
| Warmup cron not scheduled | ✅ CLOSED | `leadflow-warmup-tick` cron active, 24 successful runs in last 24h |
| `warmup_threads` empty | ✅ CLOSED | DB probe: 12 rows (4 senders × 3 partners directed full mesh) |
| `last_warmup_send_at` NULL | ✅ CLOSED | All 4 mailboxes show warmup sends within last 9 hours |
| `email_send_events` warmup-tagged | ✅ CLOSED | 20 rows with `metadata->>'kind'='warmup'` |
| 14-day timing block | 🟡 OPEN (operational) | Earliest safe cold-send = 10/06/2026 — see P0-02 |
| Migration 20260519000003 docstring "no worker yet" | ✅ CLOSED | Worker exists |

### Org-hygiene audit (26/05/2026) — 4 orgs → 1

| Finding | Status | Evidence |
|---|---|---|
| 4 orgs in DB | ✅ CLOSED | Live SQL probe: `SELECT count(*) FROM orgs = 1` (only `5557189e Purezza AU`) |
| FK CASCADE coverage | ✅ CLOSED (verified at apply) | — |
| `auth.users` row for `jordan@purezza.com.au` left intact | ✅ As designed | — |

### Platform hardening pre-cold-send (21/05/2026) — verdict was PASS w/ caveat

| Finding | Status | Evidence |
|---|---|---|
| Item 1: `sender_inboxes.last_send_at` update | ✅ CLOSED + SUPERSEDED | `sender_inboxes` dropped in PR #85 (`6b797de`); `email_accounts.last_send_at` is sole truth |
| Item 2: Smoke v2 (Management API) | ✅ CLOSED | PR #84 (`1dcf8db`) manifest-driven audit shipped 28/05 |
| Item 3: Overlay v2 mount-detection | ✅ CLOSED | shipped `e281361` — mount-detection in `main.tsx` |
| Item 4: Sender pipeline dry-run | ✅ CLOSED | — |
| Item 5: Sourcing live test deferred | 🟡 OPEN | demo-cred blocker |
| Item 6: Reply classifier dry run | ✅ CLOSED | — |
| Item 7: Analytics dashboard SQL probe | 🟡 OPEN (visual only) | demo-cred blocker |
| Item 8: Cron health 0 failures | ✅ CLOSED | 16 active crons, 0 failures in 24h |
| Caveat: clear synthetic-bounce backlog | 🟡 OPEN | See P2-22 |

### Additional Phase-5 items

| Item | Status |
|---|---|
| Smoke v2 (Management API) | ✅ CLOSED — `e281361` + PR #84 manifest-driven |
| Overlay timing v2 | ✅ CLOSED — `e281361` |
| Codex review on PR #90 | 🔴 OPEN — quota-skipped; see P1-OPS-01 |

---

## Recommended dispatch order

Top 5 next builds, in order. Format ready to drop into a `dispatch-watch.sh` BUILD prompt.

1. **BUILD-spam-act-sender-block-update** (P0-01) — replace placeholder ABN/PO Box in `users.spam_act_sender_block` with Jordan's real ABN + real reply address; smoke that a draft generated post-update renders the correct footer end-to-end. **15 min**, single migration + smoke.

2. **BUILD-edge-secrets-cold-send-essentials** (P1-ENV-02/03/04) — `supabase secrets set` for `EMAIL_VERIFICATION_API_KEY`, `EMAIL_VERIFICATION_PROVIDER`, `PIXEL_BASE_URL`, `PUBLIC_APP_URL`; redeploy affected functions; smoke a draft round-trip + bundle-grep that pixel + unsub URLs point at brand domain. **30 min**, no code change.

3. **BUILD-sentry-source-maps-and-edge-observability** (P1-ENV-01 + P1-OBS-02) — add `@sentry/vite-plugin` to `vite.config.ts`, set 3 Sentry build env vars in Vercel; add `@sentry/deno` init to `send-via-smtp` + `drain-send-queue` + `send-warmup-tick` + `poll-replies`. Throw a test error, verify symbolicated stack in Sentry. **2 hours**.

4. **BUILD-icp-gating-or-scope-decision** (P0-03) — first 30 min: confirm with Jordan whether LGA + employee-band gating is in v1 scope or deferred. If in: 1–2 day BUILD (`lead_searches.lga_allowlist text[]`, employee enrichment via Apollo/Proxycurl, CHECK constraint on categories). If out: 5-min IDENTITY.md edit documenting the decision + close P0-03. **Decision-gate first.**

5. **BUILD-admin-workers-cold-send-coverage** (P1-OBS-01) — add 5 entries to `WORKER_EXPECTED_INTERVALS`, add warmup-specific widget reading `email_send_events WHERE metadata->>'kind'='warmup'`, add reply_scan widget reading `reply_scan_runs`. **1 hour**.

---

**Next action:** P0-01 (Spam-Act block update) is a 15-min fix and blocks legal compliance — recommend dispatching that first, then the env-secrets batch (P1-ENV-02/03/04), before any other work. Cold-send timing gate (P0-02) is wall-clock only — no action accelerates 10/06/2026. P0-03 (ICP) is a Jordan-decision-gate, not an engineering blocker.
